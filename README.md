# Subin Shakya — Portfolio

A single-page portfolio built as one continuous scroll over a fixed backdrop.

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # -> dist/  (Cloudflare Pages: see cloudflare-pages.yaml)
npm run lint
```
